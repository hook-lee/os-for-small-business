# Onmove 정본 (定本)

> **정본 = "지금 무엇이 맞는지" + "어떻게 여기까지 왔는지"를, 외부 참조 없이 한 권으로 정리한 문서.**
> 이 프로젝트의 공식 설명서 한 권. 사람도 AI도 여기서 기준을 잡는다.

- **최종 갱신:** 2026-06-09
- **현재 배포 커밋:** `75b118d`
- **한 줄:** 운동 센터(필라테스/PT/요가) 사장님이 회원·강사·매출·세금을 한 곳에서 관리하는 멀티테넌트 SaaS.

---

## 0. 이 문서를 쓰는 법 (메타)

**왜 정본인가** — 실험·기능 추가·폐기가 반복되면 흔적이 정리 안 된 채 쌓인다. 그러면 AI는 전부 읽고 해석하려다 길을 잃고, 사람도 "지금 뭐가 맞는지"를 못 찾는다. 정본은 그 혼란을 막는 **단 하나의 기준점**이다.

**진실의 우선순위 (충돌 시)**
1. **사용자(이창환)의 명시적 지시** — 항상 최우선
2. **이 정본** — 현재 상태의 단일 진실 소스
3. `AGENTS.md` (멀티테넌트·도메인·보안 룰) → `CLAUDE.md` (작업 모드)
4. `docs/*` 세부 문서 (PRD·ARCHITECTURE 등) — 깊은 참조용
5. 코드 — 최종 사실. 정본과 코드가 다르면 정본이 틀린 것 → 정본을 고친다.

**갱신 규칙 (정본이 살아있으려면)**
- 새 기능·결정·방향 전환이 생기면 **그 작업 끝에 정본의 해당 섹션을 갱신**한다.
  - 기능 추가 → §2 기능 지도 / §2 데이터 모델
  - "왜 이렇게 바꿨나" → §3 변경 이력
  - 다음 할 일·미해결 → §4
  - 맨 위 "최종 갱신 / 배포 커밋"도 함께 수정
- **비밀번호·API 키·service_role 키는 절대 여기 적지 않는다.** (`.env.local` 밖으로 나가면 안 됨)
- 길어지면 `정본/` 폴더로 1·2·3·4를 파일 분리한다. (지금은 한 권 유지)

---

## 1. 지금 무엇이 맞는지 — 현재 환경

### 1-1. 정체성
| 항목 | 값 |
|---|---|
| 제품명 | **Onmove** (온무브) |
| 무엇 | 운동 센터 사장님용 통합 운영 SaaS (회원·수업·강사·매출·세금·AI 비서) |
| 첫 사용자 | **라파 필라테스** (`raphapilatesyj@gmail.com`) — 연 매출 1억대 |
| 만든 사람 | 이창환(기획·개발) — 비개발자, 여자친구 유진(라파 원장)을 위해 시작 |
| 확장 | 추가 가입 가능한 멀티테넌트 — 모든 사장님이 본인 데이터로 운영 |

### 1-2. 기술 스택 (정확한 버전 = `package.json`)
| 영역 | 기술 |
|---|---|
| 프레임워크 | **Next.js 16.2.6** (App Router, `src/`) · React 19.2.4 · TypeScript strict (`any` 금지) |
| 스타일 | **Tailwind CSS v4** (config 파일 없음, `globals.css`에 `@import "tailwindcss"`) |
| DB·인증 | **Supabase** (Postgres + Auth + RLS) — `@supabase/ssr`, `supabase-js` |
| AI 비서 | **Google Gemini** (`@google/generative-ai`) — 현재 **조회 전용** |
| 차트 | Recharts | 드래그(수업 이동) | `@dnd-kit/core` | 날짜 | date-fns |
| 테스트 | **Vitest** — 순수 분석 함수 단위 테스트 (현재 **309 tests / 35 files** 통과) |

### 1-3. 어디서 도는지
| | |
|---|---|
| 배포 | **Vercel** → `os-for-small-business.vercel.app` (push하면 자동 배포) |
| 저장소 | GitHub (origin `main`) |
| 코드 루트 | `C:\Users\leech\dev\business-os\workspace` ⚠️ (부모 `business-os`가 아님) |
| 로컬 dev | `npm run dev` |

### 1-4. 작업 끝낼 때 검증 게이트 (3개 다 통과해야 함)
```bash
cd /c/Users/leech/dev/business-os/workspace   # ⚠️ 반드시 workspace로 이동
npx tsc --noEmit
npx vitest run --reporter=dot
npx next build
```
> ⚠️ **함정:** Bash 기본 위치가 부모 폴더라, `next build`를 그냥 돌리면 "Couldn't find any pages" 에러. 항상 `cd .../workspace &&` 붙일 것.

### 1-5. DB 마이그레이션 운영 방식
- **사용자가 직접** Supabase SQL Editor에서 실행 (Claude가 DB 접근 안 함).
- 모든 SQL은 **멱등** (`add column if not exists`, `drop policy if exists`) → 여러 번 실행해도 안전.
- 누적 파일: `supabase/PENDING-MIGRATIONS.sql` (현재 v2.6 ~ **v3.22**).
- **현재 미실행 가능성 있는 것:** v3.22 (`pass_products`/`passes`의 `max_suspend_days`). 정지 기능 쓰기 전 실행 필요.

### 1-6. 디렉토리 지도
```
workspace/
├─ 정본.md                ← (이 문서)
├─ AGENTS.md              멀티테넌트·도메인·보안·금지 룰 (가장 중요한 룰북)
├─ CLAUDE.md              작업 모드 (한국어/커밋 규칙) → AGENTS.md include
├─ src/
│  ├─ app/                30개 페이지(라우트) + api/ (50+ route)
│  ├─ components/         UI (Sidebar, MobileTabBar, ui/Icon, Charts, HomeCards…)
│  └─ lib/                순수 로직·DB 접근 (아래)
│     ├─ analytics/       월별요약·세금history·알림 등 — Vitest 단위테스트 대상
│     ├─ tax/             부가세·종소세·예비비·납부기한 엔진
│     ├─ supabase/        테이블별 CRUD + auth-server/auth-browser + client(service_role)
│     ├─ ai/              tools.ts(비서 도구) · gemini.ts · system-prompt.ts
│     ├─ contracts/       전자계약 기본 템플릿·렌더
│     ├─ categories/      카테고리 정규화·기본 24개
│     └─ profile/ data/ dates/ lessons/ security/ sheets/ storage/ advice/
├─ supabase/              schema.sql · v3-saas-migration.sql · PENDING-MIGRATIONS.sql
├─ scripts/               import-rapha-data(라파 xlsx) · audit-revenue-diff
└─ docs/                  PRD·ARCHITECTURE·DECISIONS·IA·USER-FLOWS·ROADMAP·design-system
```

---

## 2. 제품 — 무엇을, 누구를 위해

### 2-1. 핵심 원칙 (절대 깨지면 안 되는 것)
> ⭐ **0. Onmove는 특정 센터(라파)용이 아니라 모든 운동센터 사장님용 범용 SaaS다.** 라파는 **'첫 계정'일 뿐 설계 기준이 아니다.** (2026-06-11 사용자 명시: "이제 라파를 아예 잊어도 돼. 어떤 원장이 쓰더라도 커스텀 가능하게.") → **모든 규칙·값·분류·문구·종류(시급 종류·결제수단·카테고리·차감/급여 정책·알림 임계치·예약 룰 등)는 원장이 커스텀할 수 있어야 한다.** 코드에 박힌 한국어 키워드 매칭·고정 상수·라파 가정은 전부 '설정화 대상'(§4-1b 백로그 참조). 새 기능 만들 때 "이 값이 센터마다 다를까?" 항상 자문하고, 다르면 처음부터 설정으로 뺀다.

1. **모든 데이터는 원장이 설계한다.** 카테고리·수강권·세금 타임라인·목표·시급 종류·정책 등을 코드에 하드코딩하지 않는다. (원칙 0의 구체화)
2. **멀티테넌트 격리.** 모든 사용자 데이터 테이블에 `owner_id`. 쿼리에서 `owner_id` 필터를 빼면 **데이터 누출**.
3. **한 계정 데이터(실명·매출·히스토리)를 다른 계정에 절대 노출 금지.** 코드·예시·시드에 특정 센터 실명/값을 박지 않는다. (라파 PII 제거 완료 — '유진 급여'→'대표자급여' 등)
4. **비개발자 친화 + 모바일 반응형 항상.** 모든 화면은 PC·모바일 둘 다 안 깨지게.
   - **숫자는 박스를 넘치지 않는다(불변 규칙).** 금액은 `@/components/ui/StatNumber`로 표시(보수적 폰트 + `원` 줄바꿈 안전밸브). 지표 카드 줄은 고정 `md:grid-cols-N` 대신 **리플로우 그리드** `grid grid-cols-2 sm:grid-cols-[repeat(auto-fit,minmax(170px,1fr))]` — 사이드바를 펼쳐 본문이 좁아져도 칸이 줄지 않고 줄 수가 줄어 절대 안 넘침. 큰 숫자 폰트는 `text-2xl` 이하 권장.

### 2-2. 기능 전체 지도 (현재 살아있는 라우트)
| 영역 | 경로 | 하는 일 |
|---|---|---|
| **홈** | `/` | 대시보드 — 처리 필요(만료임박·잔여부족)·세금 카드·목표 달성률 |
| **회원** | `/members`, `/members/[id]` | 회원 목록(자동 상태분류)·상세(수강권·운동일지·상담·강사별 시급) |
| 상담 | `/consultations` | 신규/잠재 상담 기록 → 회원 전환 추적 |
| 메시지 | `/messages` | 회원·강사 대상 메시지 발송 기록 |
| **수업** | `/lessons`(전체), `/lessons/groups`(그룹 예약 관리) | 일/주/월 통합 뷰·강사별 컬럼·드래그 이동·룸 충돌 체크 |
| 수강권 상품 | `/pass-products` | 수강권 상품 설계(카테고리·가격·최대 정지일수) |
| **강사** | `/instructors`, `/instructors/[id]` | 강사 관리·시급·로그인 계정 발급 |
| **재무** | `/finances`, `/finances/categories` | 월별 요약·카테고리(분류) 관리 |
| 매출/입력 | `/sales`, `/add` | 매출 상세·거래 입력 |
| **세금** | `/tax` | 부가세·종소세·예비비·납부기한 (과세유형 타임라인 기반) |
| 분석 | `/analytics` | 추세·비교 분석 |
| 급여 | `/payroll` | 강사 월급 정산(종류별 시급 + 회원별 조정) |
| 목표 | `/goals` | 연간 KPI 목표 vs 실적 달성률 |
| **설정** | `/settings`, `/settings/operations`, `/settings/contracts` | 개인·세무 / 운영정보(룸·알림·급여일) / 계약서 템플릿 |
| AI 비서 | `/assistant` + 플로팅 | Gemini가 데이터 들고 상담 (조회 전용) |
| 계정 | `/login`, `/signup` | 로그인·가입 |
| **회원 페이지** | `/m/[token]/*` | 회원이 토큰 URL로 보는 내 수강권·일정·그룹·계약 (로그인 불필요) |

### 2-3. 데이터 모델 (테이블 ≈ 23개, 전부 `owner_id` 격리)
| 그룹 | 테이블 |
|---|---|
| 회원·상담 | `members` · `consultations` · `member_notes`(운동일지) · `member_instructor_rates`(회원별 시급) |
| 수강권 | `pass_products` · `passes` · `pass_adjustments`(회차가감) · `pass_events`(변경이력) · `pass_suspensions`(정지) |
| 수업 | `lessons`(개인) · `group_sessions`(예약형) · `group_reservations` · `rooms` |
| 강사·급여 | `instructors`(+`auth_user_id` 로그인) · `payroll_records` |
| 재무·세금 | `transactions` · `expense_categories` · `profile`(세금 설정·목표·알림 등 집약) |
| 소통·계약 | `message_records` · `contract_templates` · `contracts` · `chat_sessions` · `chat_messages` |

### 2-4. 핵심 도메인 룰 (자주 까먹는 것 — 상세는 `AGENTS.md`)
- **분류의 source of truth = 각 거래의 `transactions.classification`** (`business`/`living`/`capital`/`reserve`/`owner_draw`). 카테고리 이름 하드코딩 금지.
- **매출 = `transactions`만 합산.** `passes.payment_amount`는 매출 통계에 절대 더하지 않는다 (이중집계). 수강권 발급 시 transactions에 매출을 자동 생성해 정합성 유지(v3.7).
- **세금:** 간이/일반 과세유형 타임라인은 **원장이 `/settings`에서 입력** (`tax_start_month`, `tax_general_since_month`). 간이 부가세 = 매출×3%, 일반 = 매출세액−매입세액.
- **수업 차감:** `completed`/`cancelled_same_day`/`noshow`만 잔여 차감. `scheduled`/`cancelled_advance`는 차감 안 함.
- **정지(suspension):** '정지'는 만료가 아니라 '잠시 멈춤' → 회원은 여전히 활성. 정지일수만큼 만료일 자동 연장. (예전 '정지중' 텍스트를 만료로 오분류하던 버그 수정 완료)
- **결제수단:** `카드`/`계좌이체`/`현금` 3개만 표준. `네이버페이` 등은 `카드`로 매핑.

### 2-5. 보안 룰 (상세는 `AGENTS.md` §보안)
- `service_role` 키는 **server-side만**. 클라이언트(`auth-browser.ts`)는 anon key만. `NEXT_PUBLIC_`로 export 금지.
- AI 비서 도구는 **회원 phone 자동 마스킹**(`maskPhone`).
- `owner_id` 필터 누락 = 데이터 누출 → 모든 쿼리에서 확인.
- 전 사용자 테이블 **RLS 활성화**. service_role은 자동 우회(앱은 영향 없음).
- Rate limit: `/api/chat` 분당 10·시간당 60, `/api/auth/signup-check` IP 분당 3.

---

## 3. 어떻게 여기까지 왔는지 — 변경 이력

> 130개 커밋의 큰 줄기. "왜 바뀌었나"가 핵심. 세부는 `docs/DECISIONS.md`.

### 3-1. 마일스톤 타임라인
| 시기/버전 | 한 일 | 왜 |
|---|---|---|
| 초기 | Next.js 부트스트랩 → 라파 xlsx(2,917건 거래)를 월별 대시보드·세금으로 이식 | 엑셀 운영을 앱으로 |
| 문서화 | AGENTS/CLAUDE/PRD/스킬 하네스 구축 + XSS 보안 | AI 협업 기준 + 안전 |
| ~v3.4 | 수업 통합 뷰(일/주/월·강사별·색·호버) + **룸 관리**(충돌 차단) | 분산된 일정을 한눈에 |
| v3.5~3.6 | **상담** 도메인 + **수강권 변경 이력**(audit) + 수강권 카테고리 | 신규 전환 추적·이력 보존 |
| v3.7 | 수강권 발급 → **가계부 매출 자동 연결** | "가계부 < 수강권 결제" 불일치의 구조적 해결 |
| 멀티테넌트화 | 월별요약·세금 **하드코딩(라파 히스토리) 제거** → 계정별 입력 기반 | 신규 가입자에게 남의 사업이 보이던 문제 |
| 세금 엔진 | 부가세 연간 통일·종소세 교정(연금저축·지방세·인적공제) + v3.10 인적공제 입력 | 세금 숫자 정확성 |
| v3.9 | 회원별 강사 시급·인센티브 → 월급 정산 우선 적용 | 회원×강사 단위 금액 차이 반영 |
| v3.11 | **연간 목표 대시보드** + 강사 성과 비교 | 원장이 목표 대비 관리 |
| 모바일 | 하단 탭바 + 전 화면 모바일 카드화·반응형 + alert→토스트 | 폰에서 쓰는 사장님 |
| PII | 라파 실명 누출 제거('유진 급여'→'대표자급여') + 강사 N+1 제거 | 멀티테넌트 안전·성능 |
| v3.16~3.17 | **회원 운동일지** + **강사 로그인**(스튜디오 컨텍스트·권한 가드) | 강사가 직접 기록 |
| v3.18 | **수강권 정지** 정식 기능 + '정지중' 오분류 버그 수정 | 여행·부상 일시정지 |
| — | 회원 등록 시 **첫 수업(체험) 일정** → 시간표 자동 반영 | 체험을 일정에 |
| v3.19~3.21 | **전자계약** (템플릿 편집 → 발송 → 손글씨 서명/직접 동의/첨부) | 회원·강사 계약 디지털화 |
| **디자인 리뉴얼** (현재) | 디자인 시스템 v1(Button/Tabs/Badge) → **좌측 사이드바** → **다크/라이트 테마** → **아웃라인 아이콘 + 하위메뉴 항상 펼침** | UI 톤 통일·현대화 |

### 3-2. 기억해 둘 결정 (되돌리지 말 것)
- **과거 거래(2024~25, 2,917건) 임의 삭제 금지** — xlsx 전체가 source of truth.
- 과거 import분 매출은 **백필 안 함** — 이미 가계부에 실제 매출이 있어 백필하면 이중집계.
- `Pass.maxSuspendDays`는 타입상 **optional**로 둠 — 테스트 픽스처 6개 수정을 피하기 위한 의도적 선택.
- 다크모드는 **토큰 전면 마이그레이션이 아니라** `html.dark` + Tailwind 유틸 오버라이드 방식(빠른 접근). 세부 색은 점진 개선 대상.

---

## 4. 할 일 · 미해결 · 첨부

### 4-1. 다음 작업 (우선순위)
> ✅ **CSV 마이그레이션 임포트 — 완료** (`/settings/import`, 설정▸데이터 가져오기). 회원·강사·수업·매출/지출 4종을 CSV로 가져옴(템플릿 다운로드·미리보기·행별 오류 리포트). 가입도 **누구나 개방**됨. 구현: `lib/csv/parse.ts`·`lib/import/schema.ts`·`api/import`.

1. **강사 급여 — 대량이체 파일 export** (송금 블로커 우회 · 현재 가능) — 앱이 돈을 직접 만지지 않고, 월 급여 명세를 **은행 기업뱅킹 대량이체(급여이체) 양식 파일**(CSV/엑셀)로 내보냄 → 원장이 본인 은행에 업로드해 한 번에 이체. 파일명에 '급여' 문구 등 급여인정 반영.
2. **② AI 음성 명령** (보류 — 사용자: "다음에") — 회원 등록·일정·강사·계약을 말/글로. 쓰기 도구(createMember 우선) + **확인 게이트 필수** + 음성(Web Speech API). 안전 민감 → **새 대화에서** 진행.
3. 다크모드 세부 색 다듬기 (포인트색·그라데이션이 어색한 곳).
4. **연동 감사 잔여(낮은 우선순위)** — ① 휴면(dormant) 회원 필터가 회원 목록에 없어 알림 '휴면' 링크는 전체목록으로 이동 ② 수강권 결제금액 사후 수정 시 연결 매출 미반영(현재 편집 UI가 금액을 안 보내 실사용 경로는 닫힘) ③ 그룹세션 'hard delete' 시 차감된 회차 미복원(→ '취소(폐강)' 사용 권장).

### 4-1b. 하드코딩 → 센터 설정화 백로그 (2026-06 감사)
> 정본 §2-1 #1("원장이 설계") 위반 또는 미연결. 우선순위 순. (법령 세금값은 고정이 정상 — 제외)

1. 🔴 **운영설정 ~24개가 placeholder** — `studio-settings`의 31키 중 실제 로직 연결은 4개(취소마감 #01·차감없는취소·급여반영 #11)뿐. 예약/예약대기/폐강/회원앱 다이얼은 **회원 셀프예약 기능 부재로 저장만 됨**. 폼의 "즉시 적용됩니다" 문구가 사실과 다름 → 최소 조치: 미연결 카드에 **"준비중" 배지** + 문구 수정.
2. 🔴 **카테고리→classification 코드 고정** — `api/transactions`·import·sheets가 `classify()`(normalize.ts:65 하드맵)로 분류를 덮어써 **원장이 /settings에서 지정한 `expense_categories.classification`을 무시**. 닫힌 `Category` union(domain.ts) + 미지 카테고리 '기타' 소실도 동반. → 쓰기·세금 경로가 DB의 classification/vatDeductible/incomeTaxDeductible를 참조하게. (마이그 0)
3. 🔴 **만료임박 7일 / 휴면 60일 알림 윈도우 리터럴 고정** — `member-segments`는 파라미터(windowDays/thresholdDays) 받는데 호출처 3곳(page/messages/notifications route)이 7·60 박음. → `lowRemainingThreshold` 패턴 복제(profile 컬럼 2개 + NotificationSettingsForm 입력란 2개).
4. 🟠 **급여 종류 키워드 매칭** — `payroll-auto.ts:52` passName에 '재활/듀엣/그룹' 포함으로 급여버킷 결정. 수강권 이름 다르면 급여 오분류. → `pass_products`에 명시적 payroll_category 지정.
5. 🟠 **차감 상태(노쇼·당일취소) 코드 상수** — `lessons.ts:5`/`deduction-rules.ts:3` 중복 정의. 노쇼 차감 토글 불가. → 설정 연동 + 중복 통합.
6. 🟠 **폐강 사유 '인원 부족' 정확일치** — `instructor-scorecard.ts:133`. 자유텍스트라 다른 표현이면 폐강률 0%. → enum/플래그화.
7. 🟡 (확장 시) 강사 시급 4종 고정·결제수단 3종 고정·그룹 정원4·50분 기본값. 라파엔 충분 → 2번째 센터 온보딩 전 재검토.

### 4-2. 대기 중 (사용자 액션 필요)
- **Supabase에서 `PENDING-MIGRATIONS.sql` 실행** — 최신 **v3.23**(`group_sessions.cancel_reason`/`cancelled_at` — 그룹수업 취소사유·폐강률), v3.22(`max_suspend_days`), v3.21(계약 첨부). 멱등이라 통째로 다시 RUN 해도 안전.

### 4-3. 블로커 (현재 불가 — 우회/대기)
- **네이버 예약 API**: 외부 개발자에게 비공개(파트너 전용) → 자체 예약폼으로 대체.
- **강사 급여 앱 내 직접 송금 / 잔액 충전형**: '사이트에 충전 후 송금' = 선불전자지급수단 → **전자금융업 등록** 필요(상법상 회사·전산인력 5명↑·자기자본 요건·총발행잔액 30억↑ 시 의무). 토스페이먼츠 **지급대행 API**는 가능하나 **사업자등록 + KYC 심사 + 수수료** 필요. → 사업화(법인/사업자) 전엔 불가. 그 전까진 **4-1의 '대량이체 파일 export'로 대체**.
- 사업자 등록 필요 항목(카카오 알림톡·토스 결제 등): 현재 못 함 → 인앱 알림·이메일·deep link로 우회. (전역 룰 참조)

### 4-4. 문서 색인 (이 한 권에서 더 깊이 들어갈 때)
| 문서 | 내용 |
|---|---|
| `AGENTS.md` | 멀티테넌트·도메인·보안·금지 룰 (가장 중요한 룰북) |
| `CLAUDE.md` | 작업 모드(한국어·커밋 규칙) |
| `docs/PRD.md` | 제품 요구사항 상세 |
| `docs/ARCHITECTURE.md` | 시스템 구조·데이터 흐름 |
| `docs/DECISIONS.md` | 의사결정 로그 |
| `docs/IA.md` · `docs/USER-FLOWS.md` | 정보구조·사용자 흐름 |
| `docs/ROADMAP.md` | 로드맵 |
| `docs/design-system.md` | 디자인 시스템 가이드 |
| `supabase/PENDING-MIGRATIONS.sql` | DB 변경 누적 (v2.6~v3.22, 멱등) |

---

*정본 끝. 새 기능·결정이 생기면 이 문서부터 갱신한다.*
