# Smoke Test 2026-05-20

## Endpoints

| Path | Status |
|---|---|
| / | 200 |
| /tax | 200 |
| /analytics | 200 |
| /settings | 200 |
| /api/transactions | 200 (2539 transactions from fixture fallback) |
| /api/profile | 200 (default profile) |

## Notes

- Tested without `GOOGLE_SHEETS_*` env vars — fallback to `tests/fixtures/real-transactions.ts` works
- `/api/transactions` initially returned 500 because `getSheetConfig()` threw when env vars were absent with no fallback path. Fixed by adding `hasSheetConfig()` guard in `src/app/api/transactions/route.ts` that calls `loadFixtureFallback()` (dynamic import of fixture) when Google Sheets env vars are not set.
- Fixture yields exactly 2539 transactions (as documented in fixture file header).
- `cachedAt` timestamp confirmed present in API response.
- `/` HTML title confirmed: `워크스페이스 — 라파 필라테스` (Next.js RSC shell — data is client-fetched).
- `/api/profile` returns default profile JSON (no persistence layer needed for smoke).
- No errors or stack traces observed in any response.

## Sample Responses

### `GET /api/transactions` (first item)
```json
{
  "date": "2024-02-22",
  "rawCategory": "자산",
  "category": "자산",
  "amount": -100000,
  "method": "계좌이체",
  "counterparty": "콤비리포머 계약금",
  "person": "2.3% 수수료",
  "classification": "capital"
}
```
Total: 2539 transactions, `cachedAt` present.

### `GET /api/profile`
```json
{"birthDate":null,"businessAddress":null,"isYoungStartupEligible":false,"youngStartupReductionRate":0,"noranusanAnnualContribution":0,"pensionAnnualContribution":0}
```

## Fix Applied

**File:** `src/app/api/transactions/route.ts`

Added `hasSheetConfig()` helper and `loadFixtureFallback()` async function. The `transactionsCache` fetcher now checks for env vars before calling `getSheetConfig()`, falling back to the fixture when they are absent. This satisfies the requirement that the app runs without Google Sheets credentials in local dev.
